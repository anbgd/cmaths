
if (условие)
{
	elseif(условие)
}
{
	else
}


while (условие)
...
end

while A
	all(A)



цикл с постусловием есть только в октаве do-while

for i = sec


1.Вычислить значение полинома в точке x=a
взять вектор коэф, начиная со старшей сепени
аргументы (p, a)
2. Поделить по схеме Горнера p на x-a (p,a)
3. Замена переменных x = y+a (p,a)
4. a) найти левую границу корней полинома
   б) найти правую границу (p)
5. Найти методом дихотомии один из корней полинома (p)
